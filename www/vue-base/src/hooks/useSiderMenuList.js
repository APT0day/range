import { menuListRoutes } from '@/router/routes'

const siderMenuList = menuListRoutes

function filterSiderMenuList(menuListRoutes) {
    menuListRoutes.forEach(route => {

    })
}
export default menuListRoutes